{
  "name": "@appliedblockchain/base-contracts",
  "version": "0.0.1",
  "description": "Solidity contracts starter package",
  "main": "index.js",
  "scripts": {
    "lint": "eslint ./src",
    "test": "TRUFFLE_PORT=8545 truffle test",
    "migrate": "TRUFFLE_PORT=8545 rm -rf ./build && truffle compile && truffle migrate",
    "ganache": "ganache-cli --port 8545"
  },
  "author": "Jamie Allen",
  "license": "ISC",
  "devDependencies": {
    "@appliedblockchain/eslint-config": "^2.2.0"
  }
}
