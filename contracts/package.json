{
    "name": "base-app-mantle-contracts",
    "scripts": {
        "compile": "truffle compile",
        "deploy": "node ./bin/deploy && rm -r ../api/contracts/* && cp -f ./build/contracts/*.json ../api/contracts"
    },
    "dependencies": {
        "dotenv": "^6.0.0",
        "web3": "^1.0.0-beta.36"
    }
}
